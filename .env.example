PORT=3001

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=torn_tracker
DB_USER=torn_user
DB_PASS=torn_secure_pass_2026

# Admin credentials (plain text here, hashed at startup)
ADMIN_USER=admin
ADMIN_PASS=changeme

# Set to true when Torn API is blocked (routes calls through Google Apps Script proxy)
USE_PROXY=false

# Session
SESSION_SECRET=change-this-to-a-long-random-string
TORN_API_KEY=v6Yo75UQIYvWYrhT

# Local receipt testing only. Ignored when NODE_ENV=production.
RECEIPT_FIXTURE_MODE=false
RECEIPT_FIXTURE_TRADE_ID=99999999
# RECEIPT_FIXTURE_FILE=tests/fixtures/receipt-trade.json
